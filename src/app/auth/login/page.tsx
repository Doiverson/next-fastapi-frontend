'use client';

import LoginButton from '@/components/LoginButton';

export default function LoginPage() {
  return (
    <div className='z-2 absolute inset-0 flex min-h-screen items-center justify-center'>
      <LoginButton />
    </div>
  );
}
